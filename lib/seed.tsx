"use client"

// This file is no longer needed as we now fetch data directly from the API
// All seeding should be done via the database migration scripts

export function useSeedOnce() {
  // No longer needed - data comes from API
  return null
}
